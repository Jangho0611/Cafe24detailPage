# 2026-06-30 작업 로그

## 작업 시작 상태

- 시작 시점의 상태 기록은 누락됨.
- 아래 내용은 작업 중 확인한 로컬 파일, 시트 상태, 생성 결과를 기준으로 정리함.

## 오늘 작업

- 타입B 비교형 인포그래픽 제거.
- 타입B를 단일 제품 구조 인포그래픽으로 변경.
- `gas/auto_pdp_generator.gs`의 `buildTypeBPrompt()` 개선.
- 타입B에 `2단: 구조 상세 확대 영역` 추가.
- 타입B 프롬프트에서 `compareTarget` 사용 제거.
- R열(TYPE) 변경 시 값이 A/B/C일 때 `createPrompt(row)`가 자동 실행되도록 `onEdit(e)` 로직 추가.
- Apps Script 원격 배포(`clasp push`) 수행.

## 테스트 결과

- VS/비교 카드 제거 성공.
- 동일 제품 확대뷰 3개 생성 확인.

## 커밋 기록

- `757ffa3` `refactor: simplify Type B infographic structure`
- `929c883` `feat: redesign Type B infographic as single-product structure view`
