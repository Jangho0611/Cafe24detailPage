1. S열 체크
   → T열 인포그래픽 프롬프터 자동 생성
   → U열 = PROMPT_CREATED

2. node gas/run-images.js 실행 (터미널)
   → 인포그래픽 이미지 생성
   → 바탕화면 인포/{행번호}.png 저장
   → U열 = IMAGE_SAVED

3. [수동] 카페24 FTP 업로드
   → 인포/{행번호}.png
   → 카페24 경로: /web/prod_detail/infographic/{행번호}.png

4. W열 체크
   → O열 URL 자동 포함
   → GPT가 HTML 생성
   → X열 = 완성 HTML
   → U열 = HTML_CREATED

5. [수동] X열 HTML 복사
   → 카페24 상품 상세설명 HTML 모드 붙여넣기